var assert = require('assert');
var expect = require('chai').expect;
var chai   = require('chai');

chai.should();

describe('Obender finishes what it has to do.', function(){

	describe('#allKeys()', function() {
		it('returns all the keys for the object');

	});

	describe('#matchedKeys()', function(){
		it('returns the matched keys object');

	});

	describe('#object()', function(){

		it('returns the object');

		it('works as alternative syntax (#done())');

	});
});
