var assert = require('assert');
var expect = require('chai').expect;
var chai   = require('chai');

chai.should();

describe('Obender can delete.', function(){

	it('deletes matched properties');

});


describe('Obender can create', function(){

	// We will just test if it can make one kind for the samek of proving it's
	// usable and assume 'denfineProperty' works as it should for the rest.
	it('creates properties');

});